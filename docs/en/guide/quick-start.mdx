---
title: One-click Install
description: One-click install and manage ZVAgent with scripts
---

The project provides scripts for one-click install, configuration, startup, and management. Script-based deployment is recommended for quick setup.

Supports Linux, macOS, and Windows. Requires Python 3.7-3.12 (3.9 recommended).

## Install Command

<Tabs>
  <Tab title="Linux / macOS">
    ```bash
    bash <(curl -fsSL https://cdn.link-ai.tech/code/zvagent/run.sh)
    ```
  </Tab>
  <Tab title="Windows (PowerShell)">
    ```powershell
    irm https://cdn.link-ai.tech/code/zvagent/run.ps1 | iex
    ```
  </Tab>
</Tabs>

The script automatically performs these steps:

1. Check Python environment (requires Python 3.7+)
2. Install required tools (git, curl, etc.)
3. Clone project to `~/ZVAgent`
4. Install Python dependencies and ZV CLI
5. Guided configuration for AI model and channel
6. Start service

By default, the Web console starts after installation. Access `http://localhost:9899` to begin chatting.

## Management Commands

After installation, use the `zv` command to manage the service:

| Command | Description |
| --- | --- |
| `zv start` | Start service |
| `zv stop` | Stop service |
| `zv restart` | Restart service |
| `zv status` | Check run status |
| `zv logs` | View real-time logs |
| `zv update` | Update code and restart |
| `zv install-browser` | Install browser tool dependencies |

See the [Commands documentation](/en/cli/index) for more details.

<Note>
  If the `zv` command is not available, you can use `./run.sh <command>` (Linux/macOS) or `.\scripts\run.ps1 <command>` (Windows) as a fallback. Both are functionally equivalent.
</Note>
